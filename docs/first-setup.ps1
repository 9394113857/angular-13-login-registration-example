# Install Angular CLI globally
npm install -g @angular/cli

# Install specific Angular modules
npm install @angular/core@13 @angular/cli@13 @angular/compiler@13 @angular/common@13 @angular/platform-browser@13 @angular/platform-browser-dynamic@13 @angular/forms@13 @angular/router@13

# Install JWT library for Angular
npm install @auth0/angular-jwt

# Install RxJS (ensure the right version)
npm install rxjs@7

# Install Angular HTTP Client module (if not already part of your Angular package)
npm install @angular/common

# Install project-specific dependencies
npm install
